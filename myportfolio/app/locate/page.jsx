'use client';
import Locate from '@/components/Locate';
import React from 'react';

const Page = ({isDarkMode}) => {

    return (
        <Locate isDarkMode={isDarkMode}/>
    );
};

export default Page;